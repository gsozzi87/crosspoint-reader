#pragma once

// PlatformIO adds include/ globally, including for sources compiled before it
// discovers library-specific include paths. Keep the forced OOM-safe allocation
// helpers available everywhere through this stable forwarding header.
#include "../lib/Memory/Memory.h"
